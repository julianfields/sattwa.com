---
pageable: true
per_page: 10
---

= partial "nav"
.container
  - page_articles.each_with_index do |article, i|
    .row.article
      .sixteen.columns
        %h2.page-title= article.title
        %p= article.date.strftime('%b %e')
        = article.summary(250)
        %p= link_to 'Read More »', article, class: "button"
